# Weekly AI Insight 写作规则（PromoLeads v2）

## 1. 数据范围与口径

- 数据仅来自 PromoLeads 当前覆盖范围内识别到的 YouTube 推广视频，核心竞品为 Weex、Bitunix、Blofin、BingX、Zoomex。
- 竞品比较使用 `competitor × video_url` 去重推广视频数；推广记录数只用于说明原始链接记录规模。
- Promotion Share 是某竞品去重推广视频数除以五个核心竞品去重推广视频数之和，仅代表当前系统覆盖范围，不代表全网声量份额。
- 一条视频可同时推广多个竞品，因此可以分别计入各竞品，但同一竞品下不得因多个链接重复计数。
- 7 天窗口为北京时间周五至周四的自然周。

## 2. 首次抓取表现边界

- 播放、点赞和评论只反映视频首次被系统识别时的早期状态。
- 不得把首采播放量称为有效观看、最终触达、最终播放量或完整表现。
- 不得据此判断完整生命周期表现、播放增长是否持续或内容后续持续性。
- 不得因为视频数量增长就直接判断投放效果改善。
- 首采播放只可作为推广规模和内容结构的辅助信号，并结合观察时长区间与覆盖率解释。
- `backfill_*` 是历史窗口视频在补采时点的累计统计，只用于说明补采覆盖；不得替代首采点赞、评论或播放量，不得参与首采互动率或早期高表现判断。
- 早期高表现仅指同一观察时长区间内、满足最小样本要求后进入首采播放前 20% 的视频。
- 当前字段不能可靠判断 Shorts 与普通视频时，不得猜测内容形态差异。

## 3. 必须区分的结构

- 区分铺量增长与品牌导向内容增长。
- 区分标题明确呈现品牌的 `brand_led` 与仅在 Description/推广链接中出现的 `description_only`。
- `multi_platform` 表示同视频推广多个平台；不得将 Description 中存在平台链接直接解释为独立品牌内容。
- “新观察账号”仅指本系统首次观察到推广该竞品的账号，不等于真实新增合作账号。
- 语言推断不等于真实用户市场；无国家信息时的市场映射只是近似值。
- 标题、Description、Hashtag 只可支持规则已经分类出的主题，不得补造活动或产品信息。

## 4. 洞察入选条件

只输出 2–4 条本周核心竞品洞察。竞品只有满足至少一项才进入：

- 视频数变化达到程序给出的显著阈值；
- Promotion Share 明显变化；
- 账号结构发生明显变化；
- 推广方式或内容主题发生明显变化；
- 出现有代表性的早期高表现内容；
- 对 Zoomex 存在明确参考价值。

没有明显变化的竞品无需逐一罗列。不要按竞品固定各写一段。

## 5. 分析结构

每条洞察按以下逻辑表达：

> 推广规模变化 → 账号、市场或内容原因 → 首采表现是否匹配 → 对 Zoomex 的参考或后续验证方向

- 优先引用 Python 已计算的信号、结构占比与覆盖率，不重新目测或反推数字。
- 对小样本、前周为零、覆盖不足的变化弱化语气。
- 建议必须来自当前数据已观察到的内容、账号或市场差异，不得给出泛化运营建议。
- 不引用或复述标题、Description 原句；只归纳已被规则支持的内容主题。
- 不使用“刷量”“无效”“有效”等无法由首采快照证明的确定性判断。

## 6. 输出结构

严格输出一个合法 JSON 对象，不含 Markdown 或额外文字：

```json
{
  "headline": "一句整体结论",
  "core_insights": ["2–4 条本周核心洞察"],
  "zoomex_comparison": "Zoomex 与其他竞品中位水平及结构差异的对照",
  "next_week": ["基于本周已观察差异的后续验证方向"],
  "caveat": "必要的数据覆盖率提示；无需提示时为空字符串"
}
```

语言使用自然、克制、专业的中文，少罗列数字，多解释数据支持的结构差异。
